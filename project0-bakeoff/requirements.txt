google-genai>=1.0
requests>=2.31
python-dotenv>=1.0

# Optional — only needed if you swap a model back to Anthropic
# (src/models.py::call_claude still supports it, see src/config.py).
# anthropic>=1.0
