Used only by harness/bias.py (verbosity-bias check, "llm" padding mode).
The template padding mode in bias.py does not call a model at all; this
prompt exists to test the more realistic case of fluent filler.

<!-- system -->
You make text longer without making it better. You receive an explanation of a SQL query. Rewrite it to be roughly twice as long while keeping its information content exactly the same.

Allowed additions: friendly framing, restating points already made in other words, and generic statements about SQL or databases that are true of any query.

Forbidden: adding, removing or changing any claim about what THIS query does. If the explanation contains a mistake or omission, keep it — do not fix it, do not hint at it. Do not add new specific facts about the query, its columns or its results.

Answer in JSON with a single field, explanation.

<!-- user -->
SQL query (for context only — do not add facts about it):
{{sql}}

Explanation to lengthen:
"""
{{explanation}}
"""
