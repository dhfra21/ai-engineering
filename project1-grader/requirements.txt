# Intentionally empty: the harness uses only the Python standard library
# (Python 3.10+). It calls Groq's HTTP API with urllib, so there is nothing
# to pip install. Kept so "pip install -r requirements.txt" is a harmless no-op.
