# Copy to .env (git-ignored) and fill in. Free key, no card: https://console.groq.com/keys
GROQ_API_KEY=

# Optional: set to "mock" to run the whole pipeline offline with fake, hash-based
# answers (for trying things out and for the tests). Mock results go to results/mock/.
# GRADER_BACKEND=mock
